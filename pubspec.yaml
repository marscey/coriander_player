name: coriander_player
description: A new Flutter project.
publish_to: 'none'
version: 1.5.1

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.0.1 
  material_color_utilities: ^0.11.1
  window_manager: ^0.3.8
  provider: ^6.1.1
  # rust_builder:
  #   path: rust_builder
  # flutter_rust_bridge: ^2.8.0
  path_provider: ^2.0.15
  flutter_localizations:
    sdk: flutter
  ffi: ^2.1.0
  path: ^1.8.3
  audio_service: ^0.18.18
  just_audio: ^0.9.35
  material_symbols_icons: ^4.2719.1
  github: ^9.24.0
  flutter_markdown: ^0.7.1
  flex_color_picker: ^3.5.0
  filepicker_windows: ^2.1.4
  file_picker: ^10.2.0
  desktop_lyric:
    git:
      url: https://github.com/marscey/desktop_lyric.git
      ref: fe84f66ba0b0e304b05c558483b37552c906cc06
  music_api:
    git:
      url: https://github.com/marscey/music_api_dart.git
      ref: c6f3e0abd9c6295b3678ce10d30bc09debda9abe
  pinyin: ^3.3.0
  hotkey_manager: ^0.2.3
  logger: ^2.4.0
  flutter_rust_bridge: 2.8.0
  rust_lib_coriander_player:
    path: rust_builder
  http: ^1.2.0
  webdav_client: ^1.2.2
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  xml: ^6.3.0
  # 使用指定版本的media_kit库解决依赖问题
  media_kit: ^1.1.11
  media_kit_video: ^1.2.5
  media_kit_libs_windows_video: ^1.0.5
  media_kit_libs_macos_video: ^1.1.2
  media_kit_libs_linux: ^1.0.11
  media_kit_libs_android_video: ^1.0.11
  media_kit_libs_ios_video: ^1.0.11
  
  # 平台特定依赖
  device_info_plus: ^9.0.3
  package_info_plus: ^4.2.0
  
  # Windows特定依赖
  win32: ^5.1.1
  
  # macOS特定依赖
  desktop_multi_window: ^0.2.0
  
  # 平台特定条件依赖
  # Windows上使用bass库
  # 在Flutter 3.3+中，使用条件依赖可以更好地管理平台特定代码
  
  # 移动端特定依赖
  # 用于Android和iOS平台的音频处理
  # audio_session: ^0.1.16 # 移动端音频会话管理

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  integration_test:
    sdk: flutter
  ffigen: ^14.0.1

flutter:
  uses-material-design: true
  assets:
    - app_icon.ico